eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false #euraka dont regist itself
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/

server:
  port: 8761
